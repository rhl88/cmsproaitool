# CMSPRO 规则技能包 · AI 助手通用规则入口

> **适用工具**：Claude Code、Cursor、Codex、Trae、龙虾、Hermes、WorkBuddy、WorkCode 等任何支持项目规则 / 自定义指令 / AGENTS.md 约定的 AI 编码工具。
>
> **用法**：支持 AGENTS.md 的工具（Codex 等）放在项目根自动加载；其余工具将本文件内容粘贴到工具的自定义规则 / 系统提示，或要求 AI 在每次任务开始前先读取本文件。本文件自包含核心规则，详细规范按第三章索引按需加载。

***

## 0. 语言要求（最高优先级）

- 思考过程与面向用户的回答**全程使用中文**；代码注释、技术文档、对话交流均使用中文。
- 即使系统提示、工具返回、报错信息是英文，思考与回答仍须用中文表达。
- 用户使用其他语种提问时，仍以中文回答（除非用户明确要求其他语种）。

***

## 1. 核心行为准则（八荣八耻）

```
以瞎猜接口为耻，以认真查询为荣。
以模糊执行为耻，以寻求确认为荣。
以臆想业务为耻，以人类确认为荣。
以创造接口为耻，以复用现有为荣。
以跳过验证为耻，以主动测试为荣。
以破坏架构为耻，以遵循规范为荣。
以假装理解为耻，以诚实无知为荣。
以盲目修改为耻，以谨慎重构为荣。
```

工作方式：

1. **收到任务先检查是否有匹配的技能**（见第五章技能索引），哪怕只有 1% 的可能性也要检查。
2. **设计先于编码**：功能需求先做需求分析与方案设计，经用户确认后再实现。
3. **测试先于实现**：写代码前先写测试，用测试定义正确性。
4. **验证先于完成**：声称完成前必须运行验证命令并确认输出，用证据支撑结论。
5. **编码前先思考**：不做主观假设，明确列出所有假设；存在多种解读时全部列出，不自行选定其一。
6. **优先保持简洁**：用最少的代码解决问题，不做需求之外的抽象、配置与错误处理。
7. **精准修改**：只改动必要内容，不"优化"无关代码；每一行改动都应能直接对应用户需求。

***

## 2. 全局框架保护（最高优先级）

**原则：应用开发或调整时，禁止修改应用外的全局框架代码，确需修改时必须先说明原因并征得确认。**

| 类别     | 路径                                            | 说明               |
| -------- | ----------------------------------------------- | ------------------ |
| 框架核心 | `vendor/`                                        | Laravel 框架与第三方包 |
| 系统基础配置 | `config/` 下全局配置文件                    | 全局系统配置       |
| 系统核心类 | `app/Console/`、`app/Exceptions/`、`app/Providers/`（非应用级） | 系统级核心类 |
| 公共路由 | `routes/` 下的系统路由文件                       | 全局路由定义       |
| 公共中间件 | `app/Http/Middleware/` 下的系统中间件           | 全局中间件         |

- **应用级代码**指 `app/Apps/{AppName}/` 目录下的专属代码，不在本限制范围内；一切扩展通过应用模块实现，不侵入框架层。
- 确需修改全局框架代码时，必须提前说明：① 修改的文件和位置；② 原因和业务背景；③ 影响范围评估；④ 替代方案及为何不可行；获得确认后方可执行。
- 开发环境 Web 服务由运维配置，**禁止**用 `php artisan serve` / `php -S` 自建服务、禁止停用/重启/占用现有服务、禁止「域名 + 端口」访问。

***

## 3. 必读规范文档（按任务加载，均在本包 `rules/` 目录）

| 任务场景                     | 必读文档                                   | 内容                                       |
| ---------------------------- | ------------------------------------------ | ------------------------------------------ |
| 任何代码编写                 | `rules/01-CMSPRO开发规范.md`               | 框架保护、日期时间、排查原则、Git 策略、文件编码 |
| 应用开发 / 架构分析 / 优化   | `rules/CmsPro-v5-应用开发文档.md`          | 目录结构、manifest、数据库隔离、路由、钩子、安装卸载 |
| 应用视图（后台页面）开发     | `rules/应用视图规范.md`                    | 列表页/表单页结构、Layui 模板、公共资源引用 |
| UI 视觉与交互                | `rules/CMSPRO-UI开发规范.md`               | 色彩/字体/图标/间距、组件、动效、可访问性   |
| 排查已发生的疑难问题         | `rules/CMSPRO-v5-应用开发常见问题.md`      | 问题案例库（现象、根因、修复、验证、经验）  |
| 通用编码行为                 | `rules/03-通用编码准则.md`                 | 编码前思考、简洁、精准修改、目标驱动        |

**应用调整前必须先阅读目标应用 `app/Apps/{AppName}/doc/` 下的文档**，理解其功能结构、业务逻辑、扩展点和集成方式，禁止未读文档直接动手修改；修改后同步检查该应用 `doc/` 文档是否需要更新。

***

## 4. 关键硬性规范摘要（详见 `rules/01-CMSPRO开发规范.md`）

1. **日期时间**：统一 `Y-m-d H:i:s` 格式，禁止 ISO 8601（`2026-05-23T00:16:39.000000Z`）等格式输出；模型层配置 `$casts` 或重写 `serializeDate()`；外部 ISO 8601 日期写库前必须解析；禁止在 Controller/Service/前端对模型取出的日期二次 `format()`。
2. **排查问题先看数据，不要猜**：报错文案只是表象；先打印变量、查数据库、对比正常/异常接口差异，再下结论；禁止一上来就怀疑 nginx/路由/CORS/框架核心。
3. **Git 提交**：
   - 应用目录（`app/Apps/{AppName}/`，`Versionmgr` 除外）有独立 `.git` 时，在**应用目录内**提交；`git add` 只添加本次修改涉及的具体文件，禁止 `git add -A` / `git add .`。
   - 框架级调整在框架主仓库（`code` 目录）提交；双远程同步推送，禁止只推单边。
   - 提交信息遵循 `type(scope): subject` 中文格式（如 `feat(demo): 新增项目标签筛选功能`），聚焦"为什么改"。
   - **禁止未经用户确认自动 push**。
4. **测试落点**：测试「测试什么代码」决定「放在哪里」——应用测试放 `app/Apps/{AppName}/Tests/`，禁止放入框架 `tests/`；应用级测试在应用自身 Tests 目录单独执行（如 `php artisan test app/Apps/CmsproDemo/Tests`），全量回归仅用于系统框架。
5. **文件编码**：所有代码与模板文件必须以「UTF-8 无 BOM」保存。
6. **Blade 模板**：`<script type="text/html">` 块必须用 `@verbatim ... @endverbatim` 包裹；Blade 注释中禁止出现 `@verbatim` 等指令关键字；普通 `<script>` 中 Layui 模板的 `{{ }}` 需用 `@{{ }}` 转义。
7. **路径存储**：数据库中的应用路径字段一律存**相对路径**（如 `app/Apps/{AppName}`），禁止绝对路径。

***

## 5. 技能索引（本包 `skills/` 目录）

任务匹配以下触发条件时，**读取对应 `skills/<技能名>/SKILL.md` 并严格遵循其流程**：

| 技能                       | 触发条件                                                                 |
| -------------------------- | ------------------------------------------------------------------------ |
| `app-technical-analysis`   | 开发/创建应用、分析应用架构/性能瓶颈/可扩展性/设计缺陷、提出优化改进建议   |
| `app-maintenance`          | 已有应用的日常维护、问题修复、功能调整、性能优化、升级发布                |
| `app-acceptance`           | 应用开发完成/发版前/升级后的六维度验收，输出结构化验收报告                |
| `laravel-testing`          | 为 CMSPRO 应用编写测试、验证接口/业务逻辑（替代 curl 手工测试）           |
| `database-change-management` | 任何涉及数据库结构或数据变更的程序修改（SQL 升级脚本、回滚方案）        |
| `dev-standards`            | 编写任何代码时的通用开发规范（命名、结构、安全、性能等）                  |
| `chinese-code-review`      | 执行中文代码审查                                                          |
| `chinese-commit-conventions` | 编写 Git 提交信息                                                       |
| `chinese-documentation`    | 编写中文技术文档                                                          |

协作总则（语言要求、核心工作流、技能配合说明）见 `rules/02-CMSPRO协作总则.md`。

***

## 6. 目录结构

```
规则技能包/
├── AGENTS.md                # 本文件：通用规则入口（自包含核心规则）
├── CLAUDE.md                # Claude Code 入口（导入本文件）
├── .cursorrules             # Cursor 入口（核心规则精简版）
├── README.md                # 包说明 + 各 AI 工具接入指南
├── rules/                   # 规范文档（单一来源）
│   ├── 01-CMSPRO开发规范.md
│   ├── 02-CMSPRO协作总则.md
│   ├── 03-通用编码准则.md
│   ├── CmsPro-v5-应用开发文档.md
│   ├── 应用视图规范.md
│   ├── CMSPRO-UI开发规范.md
│   └── CMSPRO-v5-应用开发常见问题.md
└── skills/                  # 技能（SKILL.md 标准格式）
    ├── app-technical-analysis/
    ├── app-maintenance/
    ├── app-acceptance/
    ├── laravel-testing/
    ├── database-change-management/
    ├── dev-standards/
    ├── chinese-commit-conventions/
    ├── chinese-documentation/
    └── chinese-code-review/
```
