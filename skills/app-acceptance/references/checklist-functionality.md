# 维度一：功能完整性验证清单

> 依据：`rules/CmsPro-v5-应用开发文档.md`（下称"开发文档"）
> 应用目录：`code/app/Apps/{AppName}/`（下称"应用目录"），命令均在 `code` 目录执行
> 判定级别：**\[严重]** 一票否决 / **\[强制]** 必须整改 / **\[建议]** 优化项

***

## 1.1 目录结构与命名

| #  | 检查项                                                                                                                                               | 级别    | 检查方法                                                                                                                                                                         |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 | app\_id 采用 `开发者标识.应用标识` 格式，正则 `/^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)?$/`，**全部字符仅允许小写**（点号后也必须小写，如 `niuren.Inkblog` 违规），严禁多个点号；新应用必须含开发者标识前缀        | \[强制] | 机检：读 manifest.json 的 `id` 字段，用 PHP 执行 `preg_match('/^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)?$/', $id)` 核对（开发文档·应用命名规范规则1·正误对照示例）。曾发生真实案例：`niuren.Inkblog` 因大写 `I` 安装报错「应用ID格式无效」 |
| F2 | 目录名严格等于 `app_id_to_class_name($appId)` 输出，禁止手动调整大小写                                                                                               | \[严重] | 运行 `php -r "echo app_id_to_class_name('{app_id}');"`，输出须与实际目录名完全一致（规则2/3/4）                                                                                                  |
| F3 | 命名空间与目录名完全一致（含大小写）：PHP 文件顶部 `namespace App\Apps\{AppName};`、ServiceProvider `->namespace()`、`base_path()`、`->group()` 路径                          | \[严重] | Grep：`namespace App\\Apps\\` 核对应用目录全部 PHP 文件；交叉比对 ServiceProvider 中的四处引用（规则4·Windows 通过 ≠ Linux 可运行）                                                                         |
| F4 | 新应用 Views 与 Controllers 按 `Admin/`、`User/`、`Home/` 三端建首层子目录，首字母大写                                                                                 | \[强制] | LS 应用目录核对（开发文档第四章②）                                                                                                                                                          |
| F5 | 新应用路由文件拆分为 `Routes/admin.php`、`Routes/user.php`、`Routes/home.php`（`home-api.php` 可选）                                                              | \[强制] | LS Routes 目录核对（第四章③）                                                                                                                                                         |
| F6 | `doc/` 目录存在，与 manifest.json 同级                                                                                                                    | \[强制] | LS 应用目录核对（第二十一章19.1）                                                                                                                                                         |
| F7 | 标准目录齐备：manifest.json、ServiceProvider.php、Routes/、Controllers/、Models/、Services/、Migrations/、Views/、Config/、Assets/、doc/（Hooks.php、Install.php 按需） | \[强制] | LS 核对（第二章）                                                                                                                                                                   |

**映射规则核查表**（开发文档·各层映射规则）：

| 维度       | 期望值（以 `cmspro.blog` 为例）              |
| -------- | ------------------------------------ |
| 目录名/命名空间 | 点号分隔每段 `ucfirst()` 拼接 → `CmsproBlog` |
| 数据库表前缀   | 点号替换为下划线 → `app_cmspro_blog_`        |
| 路由前缀     | 点号替换为斜杠 → `cmspro/blog`              |
| 静态资源     | 同路由前缀 → `public/apps/cmspro/blog`    |
| 配置键      | 保持点号 → `apps.cmspro.blog`            |

## 1.2 manifest.json 规范

| #    | 检查项                                                                                                                                                                     | 级别    | 检查方法                                                              |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------- |
| F8   | 必需字段齐全：`id`、`name`、`description`、`version`、`author`、`require`（含 `php`/`cmspro` 键）、`providers`                                                                           | \[强制] | 读 manifest.json 逐字段核对（第三章）                                        |
| F9   | `version` 为语义化版本号；升级交付时 version 已提升（否则升级流程不触发）                                                                                                                          | \[强制] | 与上一发布版本比对（第三章·CMSPRO 规则）                                          |
| F10  | `require` 版本约束格式合法：`>=8.3`、`>5.0.0`、`<=2.0.0`、`1.0.0`                                                                                                                   | \[强制] | \[人工] 核对格式（第三章）                                                   |
| F11  | 依赖应用已安装且已启用（未安装报 50004、未启用报 50017）                                                                                                                                      | \[强制] | 查库：`SELECT name, status FROM apps WHERE name IN (...);`（第三章·应用依赖） |
| F12  | 应用图标可正常展示：① 根目录存在 `icon.svg`（推荐，优先于 PNG）或 `icon.png`（建议 120×120，与 manifest.json 同级）；② 无图标文件时，manifest.json `icon` 字段须提供回退（FontAwesome 类名 / Layui 类名 / 图片 URL）；①② 至少满足其一 | \[强制] | Glob 应用目录 `icon.*`；无文件时读 manifest.json `icon` 字段核对（第三章·应用图标）      |
| F12a | 图标访问接口可用：存在图标文件时，`http://<开发环境地址>/api/app/{app_id}/icon` 返回 200 且无需认证，后台应用列表能正常显示图标                                                                               | \[强制] | 浏览器访问 `/api/app/{app_id}/icon` 或后台应用列表页面目视核对（第三章·应用图标）            |
| F13  | 安装后 `apps` 表 `path` 字段为相对路径（如 `app/Apps/Versionmgr`），绝对路径将导致路由加载失败                                                                                                      | \[严重] | 查库：`SELECT path FROM apps WHERE name='{app_id}';`（第四章关键规则2）       |

## 1.3 数据库规范

| #   | 检查项                                                                                                          | 级别    | 检查方法                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------- |
| F14 | 所有应用表使用 `app_{app_id点号转下划线}_` 前缀                                                                             | \[强制] | Grep Migrations 目录 `Schema::create`，核对表名（第七章·命名规范）                        |
| F15 | 迁移 `up()`/`down()` 幂等：建表先 `Schema::hasTable()`，加列先 `hasTable()`+`hasColumn()`，down 用 `dropIfExists()` 或存在性检查 | \[严重] | 逐个读 Migrations 文件核对（第七章·迁移幂等性规范）                                          |
| F16 | 表和字段均有备注，备注说明业务含义，枚举字段列出可选值，外键字段说明关联表；无备注不予合并                                                                | \[强制] | Grep 迁移文件 `->comment(` 数量与字段数比对；`ALTER TABLE ... COMMENT` 核对表备注（第七章·备注规范） |
| F17 | 时间字段使用 `create_time`/`update_time`（非 Laravel 默认 created\_at/updated\_at）                                     | \[强制] | Grep 迁移文件核对字段名（第七章·时间字段）                                                  |
| F18 | 表包含 `id`、`status`、`create_time`、`update_time` 基础字段；主键必设                                                      | \[强制] | 读迁移文件核对（第七章·基础字段参考）                                                       |
| F19 | `uninstall()` 中禁止删除数据库表（禁止 `Schema::dropIfExists()`）；建删表完全由迁移 up()/down() 负责                                 | \[严重] | 读 Install.php（如存在）核对 uninstall 方法（第十二章·卸载规范）                              |
| F20 | `uninstall()` 包含迁移回滚兜底：require 迁移文件调 down()（逆序 array\_reverse），并删除 migrations 表记录                            | \[强制] | 读 Install.php 核对（第十二章·迁移兜底机制）                                             |
| F21 | `install()` 建议含迁移兜底（require + up() + 写 migrations 记录），使用 `__DIR__` 定位迁移文件                                    | \[建议] | 读 Install.php 核对（第十二章）                                                    |
| F22 | install.sql（如有）包含 `admin_menus.code` 字段及索引，预插入关键 migrations 记录                                               | \[强制] | 读 install.sql 核对（第十三章·安装包规范）                                              |

## 1.4 路由与 API 规范

| #   | 检查项                                                                                                                         | 级别    | 检查方法                                                                                        | <br />                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------- | :----------------------------------------- |
| F23 | 三端路由前缀正确且不混用：后台 `/admin/{app_id}` + `auth:admin`；用户端 `/user/{app_id}` + `auth:web`；前台 `/{app_id}` 公开（app\_id 点号→斜杠，严禁保留字面量） | \[严重] | 读 Routes/\*.php 核对前缀与中间件（第四章·第五章）                                                           | <br />                                     |
| F24 | URL 一律小写；目录/命名空间首字母大写                                                                                                       | \[强制] | \[人工] 核对路由定义（第四章）                                                                           | <br />                                     |
| F25 | 路由参数不使用 `int` 类型提示（会触发 TypeError），不写类型或显式 `string`                                                                          | \[强制] | Grep Controllers：`function \w+\(int \$` 排查（第五章）                                             | <br />                                     |
| F26 | 所有 API 使用 `App\Http\Responses\ApiResponse` 统一响应（非 `App\Services\ApiResponse`）；`error(错误码, 文案)` 参数顺序正确                       | \[强制] | Grep Controllers `ApiResponse::` 核对 use 引用与调用（第五章）                                          | <br />                                     |
| F27 | 写接口（创建/更新）必填字段先验证再入库，失败返回 `4xxxx` 错误码，禁止未校验数据直接写库触发 500                                                                     | \[严重] | 逐个读写接口 Controller，核对 `$request->validate()` 或 Service 层 `validateXxxData()`（第五章·API 数据验证规范） | <br />                                     |
| F28 | 数据入口不唯一时，Service 层必须兜底验证                                                                                                    | \[强制] | 核对 Service 写方法（第五章）                                                                         | <br />                                     |
| F29 | 空值规范化：NOT NULL 字符串列 null→`''` 或 unset（按有无 default）；数值列空值 unset 避免 cast 写 0                                                  | \[强制] | \[人工] 读 Service 入库前处理逻辑（第五章）                                                                | <br />                                     |
| F30 | 路由内未硬编码全局路径（子域名应用必须用辅助函数如 `forum_url()`、`main_url()` 生成链接）                                                                  | \[强制] | Grep Views：\`href="/admin                                                                   | href="/user\` 排查（第二十四章22.9第6/10条，仅子域名应用适用） |
| F31 | 子域名应用公开 API 在 `registerHomeRoutes()` 中用 `Route::domain($domain)` 循环注册                                                       | \[强制] | 读 ServiceProvider（第二十四章22.9第14条，仅子域名应用适用）                                                   | <br />                                     |
| F32 | 注册前台路由的应用在 manifest.json 声明 `home_routes`；启用 root 模式时含 `findRootPathConflict()` 冲突校验                                        | \[强制] | 读 manifest.json + SettingController（第二十三章20.10）                                             | <br />                                     |

## 1.5 钩子系统

| #   | 检查项                                                     | 级别    | 检查方法                                   |
| --- | ------------------------------------------------------- | ----- | -------------------------------------- |
| F33 | 应用级钩子注册时传入 `app_id`（第4个参数），确保禁用时自动移除                    | \[强制] | Grep `addAction\|addFilter` 核对参数（第十一章） |
| F34 | Filter 钩子接收返回值：`$data = $hookManager->applyFilter(...)` | \[强制] | Grep `applyFilter` 核对赋值（第十一章）          |
| F35 | 钩子内副作用遵循事务边界：after\_\* 钩子在事务外触发，钩子内不再依赖事务回滚             | \[建议] | \[人工] 读钩子实现（第十一章·事务边界约定）               |

## 1.6 安装/卸载/升级闭环

| #   | 检查项                                                                                                                          | 级别    | 检查方法                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------ |
| F36 | 升级采用幂等更新：菜单优先按 `(app_id, code, terminal_type)` 匹配，未命中按 `(app_id, path, terminal_type)` 兜底，保留菜单 ID 和角色关联                      | \[强制] | 读 Install.php upgrade 逻辑（第十三章）                                                                   |
| F37 | 声明 `config_groups` 时，Install.php 实现 `seedDefaultConfigs()` 并在 install()/upgrade() 中调用；以 manifest 为唯一基准三向同步（新增/保留 value/删除残留） | \[严重] | 读 seedDefaultConfigs：已存在配置用 `first()` + `update()` 不传 value，禁止 updateOrCreate 覆盖用户值（第十二章·配置同步规范） |
| F38 | 升级不覆盖用户运维字段：`parent_id`、`sort`、`visible`、`status`、`type` 不被 manifest 覆盖                                                      | \[强制] | 读菜单同步逻辑（第十三章）                                                                                    |
| F39 | `.exportignore` 文件存在于应用根目录，且排除 `Tests/`、`tests/`、`.git/`                                                                     | \[强制] | 读 .exportignore（第十六章·排除规则）                                                                       |
| F40 | 安装包命名 `{app_id}-{version}.zip`                                                                                               | \[建议] | \[人工] 核对导出包（第十六章）                                                                                |
| F41 | 卸载可逆：迁移 down() 能正确回滚全部表                                                                                                      | \[严重] | 在测试环境验证（第二十章第6条）                                                                                 |

## 1.7 菜单与配置声明

| #   | 检查项                                                                                     | 级别    | 检查方法                                             |
| --- | --------------------------------------------------------------------------------------- | ----- | ------------------------------------------------ |
| F42 | `menus`/`user_menus` 中含 children 的目录菜单不声明 `path`（禁止 `"path": "/"`、`""`）；path 仅由叶子菜单声明   | \[强制] | 读 manifest.json menus 结构（第十三章·菜单声明硬性规范）          |
| F43 | 菜单 `code` 全局唯一（目录与子菜单不重复），格式 `^[a-z][a-z0-9_]*$` 且 ≤80 字符；声明后后续版本不改 code                | \[强制] | 解析 manifest menus 校验唯一性与格式（第十三章）                 |
| F44 | `user_menus` 叶子 path 以 `/user/` 开头；用户端菜单放 user\_menus 而非 menus                          | \[强制] | 读 manifest.json（第二十章第13条）                        |
| F45 | `home_menus` 扁平结构，每项均为叶子导航且必须声明 path                                                    | \[强制] | 读 manifest.json（第十三章）                            |
| F46 | 配置组和配置项 name 仅小写字母/数字/下划线，同应用内配置项 name 唯一；不手动添加 `app_` 前缀                               | \[强制] | 读 manifest.json config\_groups（第十四章）             |
| F47 | select 类型配置项必须提供 `options` 字段（label/value 数组）；value 统一为字符串                              | \[强制] | 读 manifest.json（第十四章）                            |
| F48 | 应用层 `group_code` 与 manifest `config_groups[].name` 前缀化后的 `app_{应用id}_{group_name}` 完全一致 | \[强制] | Grep 应用代码 group\_code 与 manifest 比对（第十四章注意事项第9条） |

## 1.8 权限体系

| #   | 检查项                                                                                                | 级别    | 检查方法                              |
| --- | -------------------------------------------------------------------------------------------------- | ----- | --------------------------------- |
| F49 | 权限 `name` 必须中文；推荐对象格式 `{ "code": "xxx", "name": "中文名称" }`；code 格式 `{app_id}.{模块}.{操作}`，仅小写字母/数字/点号 | \[强制] | 读 manifest.json permissions（第十五章） |
| F50 | 后端必须通过 `can:xxx` 中间件或 `auth('admin')->user()->can('xxx')` 校验权限，前端按钮隐藏仅为 UX 优化不替代后端校验               | \[严重] | 读后台 Controller/路由核对（第十五章）         |

## 1.9 测试覆盖

| #   | 检查项                                                                                    | 级别    | 检查方法                                                                                                  |
| --- | -------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------- |
| F51 | `php artisan test` 全量回归全部通过                                                            | \[严重] | 在 code 目录运行 `php artisan test`，记录测试数/断言数/结果                                                           |
| F52 | 每个写接口覆盖 4 类验证用例：①历史报错 payload 回放 ②逐个必填字段置空（断言 40001 + 无落库）③合法空值走默认值 ④更新必填传空被拦截原记录不变    | \[强制] | 读 `app/Apps/{AppName}/Tests/` 用例核对（第五章·测试要求，参考 CmsproAiproxy/Tests/Feature/ChannelValidationTest.php） |
| F53 | 测试使用 SQLite 内存数据库 + RefreshDatabase，未污染真实数据                                            | \[强制] | 读测试基类（CMSPRO 规则）                                                                                      |
| F54 | 应用的测试用例位于 `app/Apps/{AppName}/Tests/`（命名空间 `App\Apps\{AppName}\Tests`），禁止放入框架 `tests/` | \[强制] | 检查应用目录 `Tests/` 是否存在且含用例；并确认框架 `code/tests/`（及 `tests/Apps/`）下无该应用测试（开发文档·测试存放规范）                     |

## 1.10 文档交付

| #   | 检查项                                                                                               | 级别    | 检查方法                                  | <br /> | <br />                             |
| --- | ------------------------------------------------------------------------------------------------- | ----- | ------------------------------------- | :----- | :--------------------------------- |
| F55 | doc/ 目录交付三类文档，命名 \`CmsPro-{应用名称}-{特性清单                                                            | 使用指南  | 扩展指南}.md\`，中文编写                       | \[强制]  | Glob 应用目录 `doc/*.md` 核对（第二十一章19.3） |
| F56 | 文档与代码一致：功能描述、配置项、接口、依赖关系无过期内容；manifest 版本提升后文档头部应用版本同步更新                                          | \[强制] | \[人工] 抽查文档关键章节与代码比对（第二十一章19.4）        | <br /> | <br />                             |
| F57 | 应用存在 `.git` 时根目录有 README.md，含官方地址 `https://www.cmspro.cn/apps/{应用ID}`、manifest 元信息、目录结构、doc/ 文档引用 | \[强制] | 存在 `.git` 时核对 README.md（CMSPRO 规则第3章） | <br /> | <br />                             |
| F58 | 后台设置表单中的密钥/口令/Key/密码字段，保存后通过 `value` 直接明文回显；禁止使用 `placeholder="已配置（留空保持不变）"` 占位隐藏已存值              | \[强制] | \[人工] 读 Blade 视图核对（开发文档·12A.5 关键实现要点） | <br /> | <br />                             |

