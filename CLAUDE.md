# CMSPRO 规则技能包

本项目的完整 AI 协作规则与技能位于 `docs/规则技能包/`，请先加载统一规则入口：

@AGENTS.md

补充说明：

- 技能挂载：如需技能自动触发，可将 `docs/规则技能包/skills/` 下的各技能目录复制或链接到 `.claude/skills/`（项目级）或 `~/.claude/skills/`（用户级）。
- 规则文件以 `docs/规则技能包/rules/` 为单一来源，按 AGENTS.md 第三章索引按需加载。
